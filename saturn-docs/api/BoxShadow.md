# BoxShadow

BoxShadow(spread_radius: 'float' = 0.0, blur_radius: 'float' = 0.0, color: 'object' = '#000000', offset: 'Offset' = <factory>)

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 344 行）。

## 构造

```python
ft.BoxShadow(spread_radius: 'float' = 0.0, blur_radius: 'float' = 0.0, color: 'object' = '#000000', offset: 'Offset' = <factory>) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `spread_radius` | `float` | `0.0` |
| `blur_radius` | `float` | `0.0` |
| `color` | `object` | `'#000000'` |
| `offset` | `Offset` | `<factory>` |

## 本类属性

`blur_radius`、`color`、`offset`、`spread_radius`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
