# Theme

定义页面颜色与外观的主题值。

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 164 行）。

## 构造

```python
ft.Theme(font_family: 'str | None' = None, color_scheme_seed: 'str | None' = None) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `font_family` | `str | None` | `None` |
| `color_scheme_seed` | `str | None` | `None` |

## 本类属性

`color_scheme_seed`、`font_family`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
