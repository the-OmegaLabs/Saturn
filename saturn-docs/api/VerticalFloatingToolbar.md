# VerticalFloatingToolbar

Saturn 的 VerticalFloatingToolbar 公开 API。

[← API 索引](./README.md)

源码：[`saturn/widgets/floating.py`](../../saturn/widgets/floating.py)（第 134 行）。

**基类：** [FloatingToolbar](./FloatingToolbar.md)

## 构造

```python
ft.VerticalFloatingToolbar(*items, **kwargs)
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `items` | `—` | `额外位置参数` |
| `kwargs` | `—` | `额外关键字参数` |

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
