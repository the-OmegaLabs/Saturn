# ControlEvent

控件回调收到的事件数据。

[← API 索引](./README.md)

源码：[`saturn/event.py`](../../saturn/event.py)（第 13 行）。

## 构造

```python
ft.ControlEvent(name: 'str', control: "'Control'", data: 'Any' = None) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `name` | `str` | `必填` |
| `control` | `Control` | `必填` |
| `data` | `Any` | `None` |

## 本类属性

`control`、`data`、`name`、`page`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
