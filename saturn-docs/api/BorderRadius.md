# BorderRadius

BorderRadius(top_left: 'float' = 0.0, top_right: 'float' = 0.0, bottom_right: 'float' = 0.0, bottom_left: 'float' = 0.0)

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 246 行）。

## 构造

```python
ft.BorderRadius(top_left: 'float' = 0.0, top_right: 'float' = 0.0, bottom_right: 'float' = 0.0, bottom_left: 'float' = 0.0) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `top_left` | `float` | `0.0` |
| `top_right` | `float` | `0.0` |
| `bottom_right` | `float` | `0.0` |
| `bottom_left` | `float` | `0.0` |

## 本类属性

`bottom_left`、`bottom_right`、`top_left`、`top_right`。

## 本类公开方法

| 方法 | 说明 |
| --- | --- |
| `all(cls, radius: 'float')` | — |
| `horizontal(cls, left: 'float' = 0.0, right: 'float' = 0.0)` | — |
| `vertical(cls, top: 'float' = 0.0, bottom: 'float' = 0.0)` | — |
| `only(cls, top_left: 'float' = 0.0, top_right: 'float' = 0.0, bottom_right: 'float' = 0.0, bottom_left: 'float' = 0.0)` | — |

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
