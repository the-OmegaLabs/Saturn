# MaterialExpressiveTheme

Material Expressive 配色主题。

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 174 行）。

**基类：** [Theme](./Theme.md)

## 构造

```python
ft.MaterialExpressiveTheme(font_family: 'str | None' = None, color_scheme_seed: 'str | None' = None, expressive: 'bool' = True) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `font_family` | `str | None` | `None` |
| `color_scheme_seed` | `str | None` | `None` |
| `expressive` | `bool` | `True` |

## 本类属性

`color_scheme_seed`、`expressive`、`font_family`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
