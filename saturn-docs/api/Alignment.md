# Alignment

Child alignment inside a container; -1..1 on both axes.

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 188 行）。

## 构造

```python
ft.Alignment(x: 'float' = 0.0, y: 'float' = 0.0) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `x` | `float` | `0.0` |
| `y` | `float` | `0.0` |

## 本类属性

`x`、`y`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
