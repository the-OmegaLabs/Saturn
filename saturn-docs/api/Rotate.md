# Rotate

Rotate(angle: 'float' = 0.0, alignment: 'Alignment | None' = None, origin: 'Offset | None' = None, transform_hit_tests: 'bool' = True, filter_quality: 'object' = None)

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 335 行）。

## 构造

```python
ft.Rotate(angle: 'float' = 0.0, alignment: 'Alignment | None' = None, origin: 'Offset | None' = None, transform_hit_tests: 'bool' = True, filter_quality: 'object' = None) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `angle` | `float` | `0.0` |
| `alignment` | `Alignment | None` | `None` |
| `origin` | `Offset | None` | `None` |
| `transform_hit_tests` | `bool` | `True` |
| `filter_quality` | `object` | `None` |

## 本类属性

`alignment`、`angle`、`filter_quality`、`origin`、`transform_hit_tests`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
