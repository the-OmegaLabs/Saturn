# Icon

显示 Material 图标。

[← API 索引](./README.md)

源码：[`saturn/widgets/basic.py`](../../saturn/widgets/basic.py)（第 50 行）。

**基类：** [Control](./Control.md)

## 构造

```python
ft.Icon(icon, *, color=None, size: 'float' = 24, **base)
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `icon` | `—` | `必填` |
| `color` | `—` | `None` |
| `size` | `float` | `24` |
| `base` | `—` | `额外关键字参数` |

## 本类属性

`color`、`icon`、`size`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
