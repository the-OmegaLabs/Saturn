# Divider

Saturn 的 Divider 公开 API。

[← API 索引](./README.md)

源码：[`saturn/widgets/containers.py`](../../saturn/widgets/containers.py)（第 411 行）。

**基类：** [Control](./Control.md)

## 构造

```python
ft.Divider(*, height: 'float' = 16, thickness: 'float' = 1, color=None, leading_indent: 'float' = 0, trailing_indent: 'float' = 0, **base)
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `height` | `float` | `16` |
| `thickness` | `float` | `1` |
| `color` | `—` | `None` |
| `leading_indent` | `float` | `0` |
| `trailing_indent` | `float` | `0` |
| `base` | `—` | `额外关键字参数` |

## 本类属性

`color`、`height`、`leading_indent`、`thickness`、`trailing_indent`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
