# Offset

Offset(x: 'float' = 0.0, y: 'float' = 0.0, transform_hit_tests: 'bool' = True, filter_quality: 'object' = None)

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 316 行）。

## 构造

```python
ft.Offset(x: 'float' = 0.0, y: 'float' = 0.0, transform_hit_tests: 'bool' = True, filter_quality: 'object' = None) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `x` | `float` | `0.0` |
| `y` | `float` | `0.0` |
| `transform_hit_tests` | `bool` | `True` |
| `filter_quality` | `object` | `None` |

## 本类属性

`filter_quality`、`transform_hit_tests`、`x`、`y`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
