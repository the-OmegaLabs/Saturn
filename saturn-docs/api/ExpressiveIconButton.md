# ExpressiveIconButton

Saturn 的 ExpressiveIconButton 公开 API。

[← API 索引](./README.md)

源码：[`saturn/widgets/buttons.py`](../../saturn/widgets/buttons.py)（第 405 行）。

**基类：** [IconButton](./IconButton.md)

## 构造

```python
ft.ExpressiveIconButton(icon, *, size='small', **kwargs)
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `icon` | `—` | `必填` |
| `size` | `—` | `'small'` |
| `kwargs` | `—` | `额外关键字参数` |

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
