# Card

带有表面和阴影的内容容器。

[← API 索引](./README.md)

源码：[`saturn/widgets/basic.py`](../../saturn/widgets/basic.py)（第 140 行）。

**基类：** [Container](./Container.md)

## 构造

```python
ft.Card(content=None, *, elevation: 'float' = 1, variant: 'str' = 'elevated', **base)
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `content` | `—` | `None` |
| `elevation` | `float` | `1` |
| `variant` | `str` | `'elevated'` |
| `base` | `—` | `额外关键字参数` |

## 本类属性

`elevation`、`variant`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
