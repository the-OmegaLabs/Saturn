# OutlineInputBorder

Flet 1.0 outline border used by form-field controls.

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 281 行）。

## 构造

```python
ft.OutlineInputBorder(border_radius: 'object' = 4.0, side: 'BorderSide' = <factory>, gap_padding: 'float' = 4.0) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `border_radius` | `object` | `4.0` |
| `side` | `BorderSide` | `<factory>` |
| `gap_padding` | `float` | `4.0` |

## 本类属性

`border_radius`、`gap_padding`、`side`。

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
