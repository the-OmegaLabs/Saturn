# BorderSide

BorderSide(width: 'float' = 1.0, color: 'object' = None)

[← API 索引](./README.md)

源码：[`saturn/types.py`](../../saturn/types.py)（第 271 行）。

## 构造

```python
ft.BorderSide(width: 'float' = 1.0, color: 'object' = None) -> None
```

### 参数

| 参数 | 类型标注 | 默认值 |
| --- | --- | --- |
| `width` | `float` | `1.0` |
| `color` | `object` | `None` |

## 本类属性

`color`、`width`。

## 本类公开方法

| 方法 | 说明 |
| --- | --- |
| `none(cls)` | — |

---

本页依据仓库当前公开 API 与源码生成。继承成员请查阅基类；参数表中的 `**base` 会传给基类。
